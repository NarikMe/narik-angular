import { Provider } from "@angular/core";
import { ShortcutPipe } from "./pipes/shortcut.pipe";

export const COMPONENTS: Provider[] = [];
export const EXPORT_COMPONENTS: Provider[] = [];
export const PIPES: Provider[] = [ShortcutPipe];
